# ── FLEXORI — Netlify Configuration ──

[build]
  publish = "."
  functions = "netlify/functions"

# ── SCHEDULED FUNCTIONS ──
# weekly-jobs se ejecuta cada lunes a las 9:00 AM UTC (4:00 AM Colombia)
[functions."weekly-jobs"]
  schedule = "0 9 * * 1"
